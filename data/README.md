# This directory holds data files generated by the poll-status workflow.
# - playtime.jsonl: append-only raw poll log (one sample per line)
# - playtime-summary.json: rolled-up stats consumed by the site
